<!DOCTYPE html>
<html lang="en">
    <%- include("../partials/head") %> 
    <link href="/css/styles.css" rel="stylesheet">

    <%- include("../partials/header") %>
        
    <body>
        <main>
            <br>

            <br>
            <form class="form-register" action="/users" method="POST" enctype="multipart/form-data">
                <h2>REGISTRO DE USUARIO</h2>
                <div>
   
                <ul class = "errors">

                </ul>
                </div>
                <p class="control-formulario">
                <label for="name" id="labelName">Nombre:</label>
                <input type="text" name="name" id="name" class="control" value="<%= locals.oldData ? oldData.name : null %>">
                <% if (locals.errors && errors.name) { %>
                    <p class = "error-message"> <%= errors.name.msg %></p>
                <% } %> 
                </p>

                    <p class="control-formulario">
                        <label for="lastName">Apellido:</label> 
                        <input type="text" name="lastName" id="lastName" class="control" value="<%= locals.oldData ? oldData.lastName : null %>">
                        <% if (locals.errors && errors.lastName) { %>
                            <p class = "error-message"> <%= errors.lastName.msg %></p>
                        <% } %> 
                    </p>
                    
                    <p class="control-formulario">
                        <label for="birthDate">Fecha de Nacimiento:</label>
                        <input type="date" name="birthDate" id="birthDate" class="control" value="<%= locals.oldData ? oldData.birthDate : null %>">
                    </p>
                    
                    <p class="control-formulario">
                        <label for="address">Domicilio:</label>
                        <input type="text" name="address" class="control" id="address" value="<%= locals.oldData ? oldData.address : null %>">
                    </p>
                    
                    <p class="control-formulario">
                        <label for="email">email:</label>
                        <input type="text" name="email" id="email" class="control" value="<%= locals.oldData ? oldData.email : null %>">
                        <% if (locals.errors && errors.email) { %>
                            <p class = "error-message"> <%= errors.email.msg %></p>
                        <% } %> 
                    </p>
                    
                    <p class="control-formulario">
                        <label for="myfile">Imagen de perfil:</label>
                        <input type="file" id="myfile" name="myfile"  class="control">
                        <% if (locals.errors && errors.myfile) { %>
                            <p class = "error-message"> <%= errors.myfile.msg %></p>
                        <% } %>
                    </p>                        

                    <p class="control-formulario">
                        <label for="password">Contraseña:</label> 
                        <input type="password" name="password" id="password" class="control">
                        <% if (locals.errors && errors.password) { %>
                            <p class = "error-message"> <%= errors.password.msg %></p>
                        <% } %>
                    </p>

                    <p class="control-formulario">
                        <label for="passwordConfirm">Confirmar Contraseña:</label> 
                        <input type="password" name="passwordConfirm" id="passwordConfirm" class="control">
                    </p>

                <div class="control-formulario">
                    <label>Desea recibir noticias sobre nuestros productos por email?:</label>
                    <label><input type="radio" name="newsletter" value="true" checked> Si</label>
                    <label><input type="radio" name="newsletter" value="false"> No</label>
                </div>
                <button type="submit" class="boton-formulario">ENVIAR</button>
                <button type="reset" class="boton-formulario">BORRAR</button>
                
            </form>

         

        </main>
    <br>
<script src="/js/registerValidation.js"></script> 
</body> 
        <%- include("../partials/footer")%> 
</html>